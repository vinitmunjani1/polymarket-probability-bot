# dry-run by default; set EXECUTION_MODE=live only after reviewing logs
EXECUTION_MODE=dry-run

ASSETS=BTC,ETH,SOL,XRP
WINDOW_SECONDS=300
ORDER_NOTIONAL_USD=1.00
MAX_ORDERS_PER_MARKET_WINDOW=1
DRY_CAPITAL_PER_ASSET_USD=10.00
DRY_CHARGE_RATE_BPS=0.0
DRY_FIXED_CHARGE_USD=0.0

# Strategy gates
MIN_EDGE_CENTS=3.0
MAX_SPREAD_CENTS=3.0
MIN_TIME_REMAINING_SECONDS=20
# Only place new entries in the final 2 minutes of the 5m window.
# If price touches 0.80 earlier, the bot waits instead of buying immediately.
MAX_TIME_REMAINING_SECONDS=120

# Strict v0 signal rule:
# - wait until the chosen side's executable ask reaches 0.80
# - live mode buys with py-clob-client-v2 FOK market orders
# - never trade when ask is above 0.90
MIN_ENTRY_PRICE=0.80
MIN_SIGNAL_PRICE=0.80
# Prevent chasing sudden side reversals: signal trades also need model edge.
REQUIRE_SIGNAL_EDGE_CONFIRMATION=true
MIN_CONFIRMATION_EDGE_CENTS=0.0
MAX_ENTRY_PRICE=0.90
STOP_LOSS_PRICE=0.49
TRADE_TRIGGER_MODE=signal

# Polymarket / Polygon live trading; required only for EXECUTION_MODE=live
CLOB_HOST=https://clob.polymarket.com
CHAIN_ID=137
POLYMARKET_PRIVATE_KEY=
POLYMARKET_FUNDER=
# Supported values: EOA/0, POLY_PROXY/1, POLY_GNOSIS_SAFE/2, POLY_1271/3
POLYMARKET_SIGNATURE_TYPE=POLY_1271
CLOB_API_CREDS_PATH=./clob_api_creds_v2.json
